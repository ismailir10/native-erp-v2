services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: buku
      POSTGRES_PASSWORD: buku
      POSTGRES_DB: buku
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
